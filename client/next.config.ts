import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: Next.js preserves console.log by default in production
  // If logs aren't showing, check browser console settings or network issues
};

export default nextConfig;
